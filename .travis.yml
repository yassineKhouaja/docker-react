language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t yassinekhouaja/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true yassinekhouaja/docker-react npm run test
